<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimaryDark"
    tools:context="tech.honeysharma.techbmechat.Account.SettingsActivity">

    <include
        android:id="@+id/status_appBar1"
        layout="@layout/app_bar_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/settings_image"
        android:layout_width="170dp"
        android:layout_height="170dp"
        android:layout_below="@+id/status_appBar1"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="23dp"
        android:src="@drawable/default_avatar" />

    <TextView
        android:id="@+id/settings_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/settings_image"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        android:text="Display Name"
        android:textColor="@android:color/white"
        android:textSize="24sp" />

    <Button
        android:id="@+id/settings_status_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="30dp"
        android:backgroundTint="@color/colorAccent"
        android:paddingLeft="20dp"
        android:paddingRight="20dp"
        android:text="CHANGE STATUS"
        android:layout_margin="10dp"
        android:background="@drawable/search_layout"
        android:textColor="@android:color/white" />

    <Button
        android:id="@+id/settings_image_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/settings_status_btn"
        android:layout_centerHorizontal="true"
        android:background="@drawable/search_layout"
        android:backgroundTint="@android:color/white"
        android:paddingLeft="25dp"
        android:layout_margin="10dp"
        android:paddingRight="25dp"
        android:text="CHANGE IMAGE"
        android:textColor="@color/colorPrimary" />

    <Button
        android:id="@+id/settings_delete_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/settings_image_btn"
        android:layout_centerHorizontal="true"
        android:background="@drawable/search_layout"
        android:backgroundTint="@android:color/holo_red_light"
        android:paddingLeft="25dp"
        android:layout_margin="10dp"
        android:paddingRight="25dp"
        android:text="DELETE ACCOUNT"
        android:textColor="@color/colorPrimary" />

    <Button
        android:id="@+id/settings_password_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/settings_delete_btn"
        android:layout_centerHorizontal="true"
        android:background="@drawable/search_layout"
        android:backgroundTint="@android:color/holo_green_dark"
        android:paddingLeft="25dp"
        android:layout_margin="10dp"
        android:paddingRight="25dp"
        android:text="Change Password"
        android:textColor="@color/colorPrimary" />
    <TextView
        android:id="@+id/settings_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/settings_name"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp"
        android:text="@string/default_status"
        android:textColor="@android:color/white" />

</RelativeLayout>
